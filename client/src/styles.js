import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(()=>({
    appBar:{
        borderRadius: 15,
        margin: '30px',
        display: 'flex',
        flexBasis: 'column',
        justifyContent: 'center',
        alignItems: 'center',
    }

}))